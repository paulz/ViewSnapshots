import XCTest
@testable import ExampleApp
@testable import ViewSnapshotTesting

class PerceptualDifferencesTest: XCTestCase {
    func testNeglibleDifferences() {
        let intelUrl = folderUrl().appendingPathComponent("ToggleView-intel.png")        
    }
}
